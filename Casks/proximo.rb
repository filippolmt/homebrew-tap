# This file was generated by GoReleaser. DO NOT EDIT.
cask "proximo" do
  preflight_steps do
    # The binary is unsigned; strip the quarantine attribute so macOS
    # Gatekeeper does not refuse to run it with an "is damaged" alert.
    #
    # It has to be *pre*flight: `generate_completions_from_executable`
    # runs the staged binary, and Gatekeeper kills a quarantined one
    # before it can emit a single completion script. Homebrew orders cask
    # artifacts by class, not by position in the file — `PreflightSteps`
    # is first in that order, `GeneratedCompletion` near the last — so
    # this stanza holds the constraint and `postflight_steps` would break
    # it.
    #
    # `on_macos` is the declarative `if OS.mac?`, and it is not optional:
    # under Linuxbrew a `run` that cannot find `/usr/bin/xattr` defaults
    # to `must_succeed: true` and fails the install instead of doing
    # nothing.
    on_macos do
      run "/usr/bin/xattr",
          args: ["-dr", "com.apple.quarantine", "{{staged_path}}/proximo"]
    end
  end

  version "0.15.1"

  on_macos do
    on_arm do
      sha256 "c261ecf6e9e5e0efff1179f925eecc0d20db9869643f0c1c0c9c0fdebc37fc8e"
      url "https://github.com/filippolmt/proximo/releases/download/v#{version}/proximo_#{version}_darwin_arm64.tar.gz"
    end
    on_intel do
      sha256 "35c4bdeb96504775d479ae4ef3d8c80601071cbc9262b34fe7b55c6825e7bf3b"
      url "https://github.com/filippolmt/proximo/releases/download/v#{version}/proximo_#{version}_darwin_amd64.tar.gz"
    end
  end
  on_linux do
    on_arm do
      sha256 "ff09e8e9cbbb2855d48ef6fb22b1eec4294baa57a93bbb3c2ea4a6c12195685f"
      url "https://github.com/filippolmt/proximo/releases/download/v#{version}/proximo_#{version}_linux_arm64.tar.gz"
    end
    on_intel do
      sha256 "45cb260c9b52355500e4f4ba38d4c5d1b435335c4ac8ec3327d950d89ecb2069"
      url "https://github.com/filippolmt/proximo/releases/download/v#{version}/proximo_#{version}_linux_amd64.tar.gz"
    end
  end

  name "proximo"
  desc "Local development reverse proxy with automatic DNS and trusted HTTPS"
  homepage "https://github.com/filippolmt/proximo"

  livecheck do
    skip "Auto-generated on release."
  end

  binary "proximo"

  generate_completions_from_executable "proximo", "completion"

  # No zap stanza required

  caveats <<~EOS
    Run `proximo update` after upgrading: it converges the running stack to
    this CLI version and refreshes the agent Skill. It never needs sudo, and
    `proximo up` converges the stack too if you would rather just restart.
  EOS
end
